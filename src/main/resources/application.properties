spring.application.name=TicketingSystemBE

# Server port configuration

# DataSource configuration for connecting to MySQL database
spring.datasource.url=jdbc:mysql://localhost:3306/TicketingSystem
spring.datasource.username=root
spring.datasource.password=Afrah44444

# MySQL JDBC driver configuration
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate dialect for MySQL
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# File upload size configuration
spring.servlet.multipart.max-file-size=500KB
spring.servlet.multipart.max-request-size=500KB

# JPA and Hibernate configuration
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=update

# Logging configuration for Hibernate SQL and parameter binding
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Application-specific properties

# Token expiration time in milliseconds
auth.token.expirationInMils=3600000

# Secret key for JWT token generation
auth.token.jwtSecret=36763979244226452948404D635166546A576D5A7134743777217A25432A462D